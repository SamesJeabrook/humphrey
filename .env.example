# Humphrey local environment
# Copy this file to .env and replace the placeholder with your Home Assistant token.
# Never commit .env or paste the token into config/local.config.json.

HUMPHREY_HOME_ASSISTANT_TOKEN=PASTE_YOUR_HOME_ASSISTANT_TOKEN_HERE

# Optional: use a different local config file.
# HUMPHREY_CONFIG=config/local.config.json
